# PostgreSQL Connection
# For EasyPanel: use the PostgreSQL service connection string
# For local dev: use local PostgreSQL or leave empty for JSON file fallback
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/invoice_system

# Server Configuration
PORT=3002

# Email Configuration (Gmail) - Optional
# To get an App Password:
# 1. Enable 2FA on your Google account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Create a new App Password for "Mail"
# 4. Copy the 16-character password here
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-char-app-password
